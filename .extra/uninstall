#!/bin/bash
set -e

if [[ -f "$installabs"/lib64/libcustomutils.a ]] || [[ -f "$installabs"/lib64/libcustomutils.so ]]; then
	rm -f "$installabs"/lib64/libcustomutils.a "$installabs"/lib64/libcustomutils.so
	echo "removing test objects, as they might be linked with -lcustomutils" >&2
	rm -f *.o
fi

cd libs && make uninstall
